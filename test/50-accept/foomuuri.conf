zone {
  localhost
  a
  b
  c
}

a-b {
  # not added
  drop log
}

a-c {
  # both added
  accept
}

b-a {
  # multicast dropped, broadcast accepted
  multicast drop
  accept
}

b-c {
  # empty chain, not added
}
