zone {
  localhost
  public
}

macro {
  testmacro \
    1000 \
    1001
}

localhost-public {
  tcp \
    testmacro \
    drop
  tcp $(shell cat 50-parser/portlist.txt | tr '\n' ' ') reject
  $(shell cat 50-parser/rules.txt)
}
