zone {
  localhost
  public
}

localhost-public {
  tcp 1001 log
  tcp 1002 log "abc def"
  tcp 1003 log + ":tag"
  tcp 1004 log "$(szone) => $(dzone): $(statement)"
  tcp 1005 log "new level" log_level "level crit flags ip options"
}
